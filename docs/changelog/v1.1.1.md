# v1.1.1

## Fixed
- The "Check for updates automatically" option in the Options menu never showed a checkmark, so there was no way to tell if it was on or off. It now shows one, and the menu has a minimum width so long items don't get clipped.

## Changed
- Moved the "Send feedback" link out of the Options menu into the footer, next to the app version.
