<UserControl x:Class="CareerLiveryManager.App.Views.AircraftDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="700" d:DesignHeight="450"
             Background="{StaticResource CanvasBrush}">
    <Grid Margin="40,18,40,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="CAREER ACTIVITIES" Style="{StaticResource SectionLabel}" Margin="0,0,0,6"/>
                <TextBlock Text="{Binding Aircraft.Title}" FontSize="26" FontWeight="Bold"/>
            </StackPanel>
            <Button Grid.Column="1" Content="Back" Style="{StaticResource SecondaryButton}" Command="{Binding BackCommand}"/>
        </Grid>

        <TextBlock Grid.Row="1" Margin="0,0,0,16" Foreground="{StaticResource TextMutedBrush}" FontSize="13" TextWrapping="Wrap"
                   Text="This aircraft has a different livery per Career activity. Pick one below to customize it - the others stay as they are."/>

        <ScrollViewer x:Name="GridScroller" Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Activities}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding ActualWidth, ElementName=GridScroller, Converter={StaticResource WidthToColumns}, ConverterParameter=212}"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}" Width="200" Height="248" Margin="6,6,6,6" Padding="14"
                                HorizontalAlignment="Center" VerticalAlignment="Top">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Image cell; the "CUSTOMIZED" badge floats above it without taking
                                     its own row, so cards look identical whether customized or not. -->
                                <Grid Grid.Row="0" Height="86">
                                    <Border Width="180" Height="86" Background="#0A0D11" CornerRadius="4" HorizontalAlignment="Center">
                                        <Image Source="{Binding DisplayThumbnailPath, Converter={StaticResource FilePathToImage}}" Stretch="Uniform"/>
                                    </Border>
                                    <Border HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,-8,0,0" Panel.ZIndex="1"
                                            BorderThickness="1" CornerRadius="4" Padding="6,2"
                                            BorderBrush="#23503F" Background="#0E1A16"
                                            Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVisibility}}">
                                        <TextBlock Text="CUSTOMIZED" FontFamily="{StaticResource MonoFont}" FontSize="9" Foreground="{StaticResource SuccessBrush}"/>
                                    </Border>
                                </Grid>

                                <TextBlock Grid.Row="1" Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="14" Margin="0,10,0,0"
                                           HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>

                                <TextBlock Grid.Row="2" Margin="0,4,0,0" FontSize="11" HorizontalAlignment="Center" Foreground="{StaticResource TextMutedBrush}"
                                           Text="{Binding IsInstalled, Converter={StaticResource InstalledToStatusText}}"/>

                                <Button Grid.Row="4" Content="Customize" HorizontalAlignment="Center" Margin="0,10,0,0"
                                        Command="{Binding DataContext.ChooseCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
