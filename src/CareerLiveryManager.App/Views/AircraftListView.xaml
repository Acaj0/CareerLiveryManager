<UserControl x:Class="CareerLiveryManager.App.Views.AircraftListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="700" d:DesignHeight="450">
    <Grid Margin="40,32" Background="{StaticResource CanvasBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="AIRCRAFT" Style="{StaticResource SectionLabel}" Margin="0,0,0,6"/>
            <TextBlock Text="Detected aircraft" FontSize="26" FontWeight="Bold"/>
        </StackPanel>

        <DockPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,10,0" DockPanel.Dock="Left" Foreground="{StaticResource TextMutedBrush}"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
        </DockPanel>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="{StaticResource TextMutedBrush}" Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibility}}"/>

        <ListBox Grid.Row="2" ItemsSource="{Binding Aircraft}" BorderThickness="0" Background="Transparent"
                 ScrollViewer.CanContentScroll="False" VirtualizingPanel.IsVirtualizing="False">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,10"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource Card}" Padding="14" Opacity="{Binding IsSupported, Converter={StaticResource BoolToOpacity}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Width="180" Height="86" Margin="0,0,18,0" Background="#0A0D11" CornerRadius="4">
                                <Image Source="{Binding DisplayThumbnailPath, Converter={StaticResource FilePathToImage}}" Stretch="Uniform"/>
                            </Border>

                            <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="15"/>
                                    <Border Margin="8,0,0,0" BorderThickness="1" CornerRadius="4" Padding="6,2" BorderBrush="#23503F" Background="#0E1A16"
                                            Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVisibility}}">
                                        <TextBlock Text="INSTALLED" FontFamily="{StaticResource MonoFont}" FontSize="9" Foreground="{StaticResource SuccessBrush}"/>
                                    </Border>
                                </StackPanel>
                                <TextBlock Text="{Binding VendorName, StringFormat='vendor: {0}'}" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,3,0,0"/>
                            </StackPanel>

                            <TextBlock Grid.Column="2" Text="Not supported" Foreground="{StaticResource WarningBrush}" VerticalAlignment="Center" Margin="8,0" FontSize="12"
                                       ToolTip="This aircraft doesn't use livery.cfg (old format) and isn't compatible with this tool."
                                       Visibility="{Binding IsSupported, Converter={StaticResource InverseBoolToVisibility}}"/>

                            <Button Grid.Column="3" Content="Choose"
                                    Command="{Binding DataContext.ChooseCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    IsEnabled="{Binding IsSupported}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
